tag:#basic
A **shell** is just a command line _on the target machine_ — once you have one, you can type commands the victim computer will run. Getting that first shell = your **foothold**. The most common type is a **reverse shell**: instead of you connecting in (firewalls block that), you trick the target into connecting _out_ to you.

![[reverse_shell.png]]